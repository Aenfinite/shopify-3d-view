// Firebase removed — style options are hardcoded in sample data.
import type { StyleOption } from "@/types/configurator"

export async function getStyleOptions(): Promise<Record<string, StyleOption[]>> {
  return {}
}
